{
    "version": "0.2.0",
    "configurations": [
        {
            // for MacOS
            "name": "(lldb) Launch exe",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceRoot}/opgl-imgui-cube/3dCube.exe",
            "args": [],
            "cwd": "${workspaceRoot}",
            //"preLaunchTask": "cmake",
        }
    ]
}