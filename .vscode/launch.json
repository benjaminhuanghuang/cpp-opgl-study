{
    "version": "0.2.0",
    "configurations": [
        {
            // for MacOS
            "name": "(lldb) Launch exe",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceRoot}/2d-shader/2d.exe",
            "args": [],
            "cwd": "${workspaceRoot}/2d-shader",
            //"preLaunchTask": "cmake",
        }
    ]
}